## 2026-09-12 - Add dynamic aria-labels to SpecMasterAdminClient\n**Learning:** When adding aria-labels to elements with dynamic titles, incorporating the dynamic data into the aria-label makes it significantly more useful for screen readers by providing context (e.g., "Click to activate [Item Name]" instead of just "Click to activate").\n**Action:** Always check if a title attribute is dynamic and replicate that logic in the aria-label.
